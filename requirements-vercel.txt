# FastAPI and server
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0

# HTTP client
requests>=2.31.0
tenacity>=8.2.0

# Embedding model (ONNX - minimal install for Vercel)
onnxruntime>=1.16.0
transformers>=4.35.0
tokenizers>=0.15.0

# Similarity computation
numpy>=1.24.0
scikit-learn>=1.3.0

# Utilities
loguru>=0.7.0
python-dotenv>=1.0.0

# Rate Limiting
slowapi>=0.1.9

# Note: Uses onnxruntime directly (not optimum) to reduce memory during build
# PyTorch is NOT installed - using ONNX for Vercel compatibility
