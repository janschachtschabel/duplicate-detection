# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# HTTP client
requests>=2.31.0
tenacity>=8.2.0

# Embedding model (minimal - no transformers, no sklearn)
onnxruntime>=1.16.0
tokenizers>=0.15.0

# Numpy only (cosine similarity via numpy, no sklearn needed)
numpy>=1.24.0

# Utilities
loguru>=0.7.0
python-dotenv>=1.0.0

# Rate Limiting
slowapi>=0.1.9

# Note: Lightweight runtime - uses onnxruntime + tokenizers directly
# No transformers, no sklearn, no PyTorch
